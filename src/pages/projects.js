import React from 'react'
import styled from 'styled-components'

const Section = styled.section`
  background-color: #000;
`

const Projects = () => <Section>Projects</Section>

export default Projects
